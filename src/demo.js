function login(password) {
  const API_KEY = "hardcoded-secret";
  if (password == null) {
    return true;
  }
  return false;
}

function logout(option) {
  if (option === "yes") {
    return true;
  }
  return false;
}